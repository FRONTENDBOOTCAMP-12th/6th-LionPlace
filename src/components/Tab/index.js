import '@/components/Time/time';
import '@/components/ProfileAll/profileAll';
import '@/components/Navigation/navigation';
import '@/components/Tab/tab';
import '@/components/Tab/customElement';

// const tabButtons = document.querySelectorAll('.tab-button');
// const tabContents = document.querySelectorAll('.tab-content');

// function activeBtn(target) {
//   tabButtons.forEach((btn) => btn.classList.remove('is--active'));
//   target.classList.add('is--active');
// }

// function activeCtn(targetTab) {
//   const dataTarget = document.getElementById(targetTab);
//   tabContents.forEach((content) => content.classList.remove('is--active'));
//   dataTarget.classList.add('is--active');
// }

// function handleTabBtn(e) {
//   const target = e.target;
//   const targetTab = target.dataset.tab;

//   activeBtn(target);
//   activeCtn(targetTab);
// }

// function initTabButtons() {
//   tabButtons.forEach((item) => item.addEventListener('click', handleTabBtn));
// }

// initTabButtons();
