import { css, LitElement } from 'lit';

export const findUserIdCss = css`
  .find-section {
    max-width: 30rem;
    margin: 0 auto;
    padding: 2rem;
  }

  .find-section h1 {
    margin-bottom: 1rem;
  }

  .find-section a {
    float: right;
  }
`;
