import { css } from 'lit';

export const startPageFormCss = css`
  .start-section {
    max-width: 30rem;
    margin: 0 auto;
    padding: 2rem;
  }

  .start-section app-logo {
    display: block;
    margin-bottom: 15rem;
  }

  .start-section action-button {
    display: block;
  }
`;
