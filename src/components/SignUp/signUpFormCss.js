import { css } from 'lit';

export const signUpFormCss = css`
  .container {
    max-width: 30rem;
    margin: 0 auto;
    padding: 2rem;
  }

  h1 {
    font-size: 2rem;
    font-weight: 600;
    margin-bottom: 2rem;
    color: var(--white);
  }
`;
